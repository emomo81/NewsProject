services:
  - type: web
    name: my-news-website
    env: python
    region: oregon
    buildCommand: "./build.sh"
    startCommand: "python manage.py migrate && python create_superuser.py && gunicorn MyNewsWebsite.wsgi:application"
    plan: free
    branch: main
    rootDir: Scripts/MyNewsWebsite
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: NODE_VERSION
        value: 18.17.1
      - key: REACT_APP_CURRENT_NEWS_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: my-news-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
      - key: DJANGO_SUPERUSER_USERNAME
        value: admin
      - key: DJANGO_SUPERUSER_PASSWORD
        generateValue: true
      - key: DJANGO_SUPERUSER_EMAIL
        value: admin@example.com

databases:
  - name: my-news-db
    plan: free
    databaseName: my_news
    user: my_news_user